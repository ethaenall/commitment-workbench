{
  "//": "The published-tarball emit. nodenext makes the output resolvable by a plain Node or tsc consumer — it is why relative imports in src/ carry .js extensions. The committed exports map still points at ./src/*.ts; only the packed tarball uses this output. No source maps: files is [\"dist\"], and a map naming a file the tarball does not contain is worse than no map.",
  "extends": "./tsconfig.json",
  "compilerOptions": {
    "module": "nodenext",
    "moduleResolution": "nodenext",
    "noEmit": false,
    "declaration": true,
    "outDir": "dist",
    "rootDir": "src"
  },
  "include": ["src/**/*.ts"]
}
